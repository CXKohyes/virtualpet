# 虚拟宠物养成项目 PRD

> 文档版本：v2.0  
> 文档状态：首个里程碑待开发  
> 工作名：像素宠物屋  
> 目标平台：响应式 Web（手机浏览器优先，兼容桌面浏览器）

## 1. 产品概述和目标用户

### 1.1 产品定位

像素宠物屋是一款怀旧向虚拟宠物养成游戏。玩家领养一只宠物，通过喂食、玩耍、清洁和睡觉照顾它，看着它从幼年成长为最终形态。宠物会在玩家离开时继续变化，也会用不同性格的台词回应玩家，目标是重现早期电子宠物“它一直在那里等着我”的情感体验。

产品同时承担一个工程目标：作为使用 Claude Code + DeepSeek Flash 进行 vibe coding 的完整全栈练手项目，前后端、数据库、自动化测试和 AI 代理协作规范都要真实落地。

### 1.2 核心体验

1. 领养：从猫、狗、像素龙中选择一只并命名。
2. 照顾：喂食、玩耍、清洁、睡觉，每次操作都有即时动画、音效和台词反馈。
3. 牵挂：宠物在离线期间继续衰减；回到游戏后需要处理饥饿、脏乱、疲劳或生病。
4. 成长：通过持续照护获得经验，达到等级和健康条件后进化为成长形态和最终形态。
5. 个性：不同物种有不同属性倾向和性格台词，玩家能感受到“我养的这只和别人的不一样”。
6. 对战（后续版本）：使用好友码挑战朋友宠物的快照，自动战斗并查看战报，不需要双方同时在线。

### 1.3 目标用户

| 用户类型 | 特征 | 核心诉求 |
| --- | --- | --- |
| 怀旧玩家 | 玩过拓麻歌子、电子宠物、早期掌机游戏 | 找回等待、照顾和养成的熟悉感 |
| 轻度休闲玩家 | 每天愿意打开应用 2–3 次，每次 1–3 分钟 | 低负担、有反馈、不惩罚长期离开 |
| 像素风爱好者 | 喜欢 8-bit/16-bit 视觉、复古 UI、合成音效 | 统一的复古审美和细节反馈 |
| 项目作者与 AI 编程学习者 | 使用 Claude Code 和 DeepSeek 开发 | 结构清晰、可测试、适合分批迭代的全栈项目 |

### 1.4 产品目标

- 首次打开后 1 分钟内完成领养并完成至少一次照护操作。
- 让玩家在 5 分钟内理解四类操作、五项状态和成长目标。
- 通过离线变化和性格台词建立情感连接，而不是依赖数值竞争。
- 在不引入公网服务、账号系统和运行时大模型的前提下完成可玩的本地版本。

### 1.5 非目标

- 首版不做账号注册、密码登录、第三方登录和跨设备同步。
- 首版不做实时多人对战、聊天、排行榜、公会和好友动态。
- 首版不做付费、商城、道具背包、广告和推送通知。
- 首版不接入运行时 AI 对话；DeepSeek 只用于开发过程。
- 首版不做原生 App、微信小程序和 Windows 桌面客户端。

## 2. 详细功能列表

### 2.1 匿名身份与存档

- 首次访问时浏览器生成并保存 `deviceId`。
- 前端调用匿名会话接口，服务端创建玩家并返回访问令牌。
- 令牌只保存在浏览器本地；服务端只保存令牌哈希。
- 玩家最多有 3 个宠物槽位（P2 多宠物槽）。同一时间只有一只「当前宠物」，
  四个操作、照护日志和对战都作用在它身上，可以在名册里切换。
- 非活跃宠物照常随时间衰减，没有暂停机制。
- 刷新页面、关闭浏览器、重启前后端后，宠物状态和操作记录仍然存在。
- 设置页提供“重新领养”，需要二次确认；确认后删除当前宠物并回到领养页。
- 清除浏览器存储视为新存档，MVP 不提供找回能力。

### 2.2 领养与命名

- 展示猫、狗、像素龙三只候选宠物。
- 每只候选宠物展示名称、简短介绍和特性标签。
- 玩家输入宠物名字，长度 1–8 个字符，支持中文、英文和数字。
- 名字去除首尾空格，禁止空名和纯空白。
- 创建成功后自动进入宠物主界面。
- 三个槽位全部占用时创建返回冲突错误，不覆盖任何已有存档。
- 领养时自动占用最小的空闲槽位；送走中间某只后，那个槽位能被重新用上。

### 2.3 宠物状态系统

每只宠物拥有五项 0–100 的属性。

| 属性 | 含义 | 归零影响 |
| --- | --- | --- |
| 饱食 satiety | 是否吃饱 | 健康持续下降 |
| 心情 mood | 是否开心 | 健康缓慢下降，台词变低落 |
| 清洁 hygiene | 是否干净 | 健康持续下降，台词抱怨 |
| 精力 energy | 是否疲劳 | 无法玩耍，容易进入疲劳状态 |
| 健康 health | 综合健康 | 低于 30 进入生病状态 |

状态枚举：

- `NORMAL`：正常。
- `HUNGRY`：饱食低于 25。
- `DIRTY`：清洁低于 25。
- `TIRED`：精力低于 20。
- `SAD`：心情低于 30。
- `SICK`：健康低于 30。
- `SLEEPING`：正在睡觉。

健康与生病规则：

- 饱食低于 25 时健康 -2/小时。
- 清洁低于 25 时健康 -2/小时。
- 心情低于 20 时健康 -1/小时。
- 四项核心状态均高于 60 时健康 +1/小时。
- 健康低于 30 进入生病状态，恢复到 50 以上解除。
- 健康下限为 0，但宠物永不死亡。

### 2.4 日常操作

| 操作 | 基础效果 | 使用条件 | 冷却 | 经验 |
| --- | --- | --- | ---: | ---: |
| 喂食 | 饱食 +30、心情 +3、清洁 -2 | 饱食低于 95 | 60 秒 | 6 |
| 玩耍 | 心情 +22、精力 -12、饱食 -5、清洁 -4 | 清醒且精力 ≥15 | 60 秒 | 8 |
| 清洁 | 清洁 +35、心情 +5 | 清洁低于 95 | 60 秒 | 6 |
| 睡觉 | 精力 +10/小时，饱食 -3/小时，心情 -1/小时，清洁 -1/小时 | 清醒且精力低于 70 | 醒来后结算 | 每小时 2，单次最多 12 |
| 唤醒 | 提前结束睡觉 | 正在睡觉 | 无 | 按已睡整小时结算 |

通用规则：

- 每次操作必须生成唯一 `clientRequestId`，重复提交只返回第一次结果。
- 数值全部由服务端计算，前端只负责展示和动画。
- 无效果操作返回明确原因，例如“它现在不饿”“它还不困”“它太累了”。
- 属性变化后立即刷新状态条，并播放对应反馈。

### 2.5 时间流逝与离线衰减

- 宠物状态在玩家离开后按小时继续变化。
- 每次请求由服务端结算经过时间，不依赖每分钟定时任务。
- 单次离开最多结算 12 小时，超过部分不累计。
- 睡觉期间使用睡觉衰减表；精力达到 95 或睡满 10 小时自动醒来。
- 结算结果包含状态变化摘要，用于回访弹窗和日志。

清醒状态每小时变化：

| 属性 | 变化 |
| --- | ---: |
| 饱食 | -5 |
| 心情 | -4 |
| 清洁 | -3 |
| 精力 | -4 |

### 2.6 物种特性

| 物种 | 定位 | 特性 |
| --- | --- | --- |
| 猫 | 灵巧、爱玩 | 玩耍心情收益 +25%，清洁衰减 -25% |
| 狗 | 均衡、亲人 | 所有正向照护收益 +10%，健康恢复额外 +1/小时 |
| 龙 | 贪吃、强壮 | 喂食饱食收益 +15%，精力衰减 -25% |

### 2.7 等级、进化与形态

- 宠物最高 10 级。
- 累计经验阈值：40、90、150、220、300、390、490、600、720。
- 幼年形态：1–3 级。
- 成长形态：4 级且健康 ≥60。
- 最终形态：8 级、健康 ≥70，且饱食、心情、清洁、精力全部 ≥50。
- 条件未满足时保持当前形态；条件满足后的下一次结算或操作触发进化。
- 进化不可逆，播放专属动画、音效和台词。
- 正常游玩节奏约为第 5 天到 4 级、第 16 天到 8 级。

### 2.8 性格台词

- 每只宠物按物种和当前状态播放短句。
- 状态包括正常、饥饿、脏乱、疲劳、低落、生病和睡觉。
- 每只宠物每个主要状态至少准备 3 句台词。
- 台词不连续重复；操作、升级、生病、进化和回访时优先播放场景台词。
- 台词是固定内容，不调用在线大模型。

### 2.9 音效与设置

- 使用 Web Audio 合成立方波/三角波音效，不引入音频素材。
- 喂食、玩耍、清洁、升级、生病、进化使用不同提示音。
- 音效必须由用户首次点击后触发，避免浏览器自动播放限制。
- 设置页提供静音开关，设置保存在浏览器本地。
- 设置页提供“重新领养”入口。

### 2.10 界面与适配

- 手机端使用单列布局：状态区、宠物舞台、操作区、日志区。
- 桌面端使用双栏布局：左侧宠物舞台，右侧状态、日志和设置。
- 适配 360px、768px、1440px 三档宽度。
- 操作按钮触控区域不小于 44px。
- 尊重 `prefers-reduced-motion`，关闭非必要动画但保留数值反馈。

### 2.11 后续版本：异步宠物对战

- 每个玩家获得可分享的好友码。
- 玩家输入好友码发起挑战。
- 服务端保存双方宠物快照，不读取对方实时状态。
- 服务端使用固定随机种子执行确定性自动战斗。
- 战斗结果生成回合制战报，双方可查看。
- 物种特性影响速度、攻击、防御或恢复倾向。
- WebSocket 只用于战报就绪等通知，不作为养成主链路。

## 3. 功能优先级

### MVP / P0

- 匿名会话、本地令牌、服务端存档。
- 猫、狗、像素龙三选一和命名。
- 五项状态显示。
- 喂食、玩耍、清洁、睡觉和唤醒。
- 懒结算离线衰减、12 小时上限。
- 生病与恢复，不死亡。
- 等级、经验、三阶段进化。
- 物种特性修正。
- 性格台词和操作日志。
- 8-bit 合成音效和静音开关。
- 响应式主界面和领养页。
- 程序化生成 9 张像素精灵图。
- 本机一键启动脚本、数据库迁移和自动化测试。
- WebSocket 连通性预埋。

### 后续版本 / P1

- 好友码与异步自动战斗。
- 战报查看和最近对战记录。
- 对战数值平衡与物种倾向。
- WebSocket 战报通知。
- 更多台词、动作帧和粒子效果。
- 每日照护摘要和回访报告。

### 远期版本 / P2

- 账号注册与跨设备同步。
- 实时回合制对战、房间、匹配和断线重连。
- ~~多宠物槽位。~~ **已实现**（见 2.1、2.2），不再属于远期版本。
- 道具、食物种类和收集图鉴。
- 公网部署、HTTPS/WSS、生产数据库和监控。
- 更多物种、分支进化和季节活动。

## 4. 界面设计要求

### 4.1 视觉方向

- 主风格为 16-bit 像素风，参考早期掌机界面。
- 使用深色外框、浅色内屏、粗描边和高对比色块。
- 主色建议：墨绿、米黄、暖橙、珊瑚红、天空蓝。
- 所有图像使用最近邻缩放，禁止模糊插值。
- 中文字体优先选择 OFL 授权的中文像素字体；授权不确认时回退系统字体。

### 4.2 主界面布局

1. 顶部：宠物名字、等级、当前状态和服务器时间。
2. 状态区：五条像素状态条，低值有颜色警告。
3. 舞台区：宠物精灵、地面阴影、性格气泡和粒子反馈。
4. 操作区：喂食、玩耍、清洁、睡觉/唤醒四个主按钮。
5. 日志区：最近操作、离线变化和进化记录。
6. 设置入口：音效开关、重新领养、版本信息。

### 4.3 交互反馈

- 点击按钮立即出现按压反馈，不等待网络返回。
- 操作成功后状态条平滑变化，数值跳动并播放音效。
- 操作失败时显示原因，不静默失败。
- 生病、升级和进化使用全屏或半屏强调动画。
- 离线性回访时优先展示“你不在时发生了什么”。

### 4.4 可访问性

- 状态条除颜色外必须有文字或图标说明。
- 精灵图提供替代文本。
- 键盘可聚焦所有按钮。
- 文字与背景对比度满足 WCAG AA 的基本要求。
- 动画不遮挡关键操作按钮。

## 5. 技术栈建议

| 层次 | 选型 | 说明 |
| --- | --- | --- |
| 前端 | Vue 3 + Vite + TypeScript | 组合式 API、类型安全、启动快 |
| 状态管理 | Pinia | 管理会话、宠物和 UI 状态 |
| 路由 | Vue Router | 领养页与主界面切换 |
| HTTP | Axios 或原生 fetch | 统一封装令牌、错误和重试 |
| 后端 | Spring Boot 3.5.x + Java 17 | 稳定、生态成熟、适合分层设计 |
| 数据访问 | MyBatis-Plus 3.5.x | SQL 可控，CRUD 效率高 |
| 数据库 | 本机 MySQL 5.7.44，生产建议 MySQL 8 | 沿用现有环境，减少安装成本 |
| 迁移 | Liquibase formatted SQL | 兼容 MySQL 5.7，适合版本化建表 |
| 实时通信 | Spring WebSocket + STOMP | MVP 只预埋，后续用于战报通知 |
| 测试 | JUnit 5、MockMvc、H2 MySQL 模式、Vitest | 覆盖核心规则和接口 |
| 音频 | Web Audio API | 合成 8-bit 音效，无素材依赖 |
| 美术 | Python 3.10 + Pillow | 程序化生成可复现的像素 PNG |
| AI 开发 | Claude Code + DeepSeek Flash | 用于开发，不进入游戏运行时 |

## 6. 非功能性需求

### 6.1 性能

- 本机环境下核心 API 的 P95 响应时间目标小于 300ms。
- 状态结算必须是 O(1) 计算，不随时间跨度循环。
- 前端首屏在开发环境目标 1.5 秒内可交互。
- 初始 JavaScript 资源建议控制在 gzip 后 500KB 以内。
- 精灵图单张不超过 32KB，9 张总量建议不超过 200KB。
- 离线结算只保存最终状态和摘要，不为每个小时写日志。

### 6.2 安全

- 所有数值、冷却、经验、等级和进化判定以服务端为准。
- 令牌使用安全随机数生成，服务端只保存哈希。
- 宠物名字必须做长度、空白和特殊字符校验，输出时按文本渲染，禁止直接插入 HTML。
- MyBatis-Plus 查询必须使用参数绑定，禁止拼接用户输入。
- 操作接口必须校验冷却和 `clientRequestId`，防止重复提交和刷经验。
- API Key、数据库密码和本地配置不得提交到 Git。
- 本地开发可使用 HTTP；公网部署必须使用 HTTPS 和 WSS。
- 生产环境需要跨域白名单、速率限制和安全响应头。

### 6.3 可靠性与数据一致性

- 状态更新必须在事务中完成。
- 使用乐观锁 `version` 处理并发更新。
- 重复请求返回第一次操作结果，不重复扣减或增加数值。
- 所有时间以 UTC 存储，展示时转换为浏览器本地时间。
- 服务重启后仍能根据 `lastSettledAt` 正确结算。
- 数据库迁移可重复执行且失败时给出明确错误。
- 宠物最低状态不会导致数据异常或不可恢复的死亡。

### 6.4 兼容性

- 支持最新两个大版本的 Chrome、Edge、Firefox 和 Safari。
- 支持 iOS Safari 和 Android Chrome 的常见屏幕尺寸。
- 最低适配宽度 360px。
- 不依赖鼠标悬停才能完成核心操作。
- 音效失败不能阻断游戏流程。

### 6.5 可维护性

- 业务规则集中放在后端服务层，前端不复制数值算法。
- 物种、操作、等级和阈值通过配置接口返回。
- 代码分层清晰：Controller、Service、Mapper、Domain、DTO、Config。
- 每个核心规则至少有一个自动化测试。
- 数据库结构只能通过迁移文件修改，不允许手改后忘记提交。
- 文档、接口字段和枚举名称保持一致。

### 6.6 可观测性

- 每个请求记录 `requestId`、路径、耗时和结果码。
- 关键业务事件记录结构化日志：创建宠物、操作、升级、进化、生病、重置。
- 提供 `GET /actuator/health`。
- 本地开发日志不得输出令牌、密码或完整设备标识。

### 6.7 隐私

- MVP 只收集随机设备标识和宠物数据，不收集姓名、手机号、邮箱和位置。
- 不接入第三方统计和广告 SDK。
- 重置存档时删除该玩家关联的宠物和日志。

## 7. 验收标准

- 首次打开可以选择猫、狗或龙并完成命名。
- 主界面展示五项状态、宠物精灵、四个操作和日志。
- 四种操作均有服务端计算结果、动画、音效和台词反馈。
- 刷新页面、关闭浏览器、重启服务后存档仍存在。
- 模拟 8 小时离开后状态下降但不生病死亡。
- 模拟超过 12 小时离开时最多按 12 小时结算。
- 健康低于 30 进入生病，恢复到 50 以上解除。
- 达到条件后依次进化到成长形态和最终形态。
- 三只宠物都有独立的特性修正、台词和 3 个形态精灵。
- 360px、768px、1440px 三档布局无横向滚动和关键元素遮挡。
- `mvn test`、`npm run build` 和前端测试全部通过。
- 仓库不包含任何密钥、数据库密码或生成式模型令牌。
