\*\*结论：Vue 3 负责前端渲染与交互，Spring Boot 负责核心状态机与对战逻辑，两者通过 REST + WebSocket 通信。\*\* 当前唯一需要解决的工程问题是 Claude Code 接入 DeepSeek 的环境配置——搜索结果中有一份完整的官方配置方案可以直接使用 。



\### 技术栈定型



| 层次 | 选型 | 理由 |

|------|------|------|

| \*\*前端\*\* | Vue 3 + Vite + Pinia + TypeScript | 组合式 API 适合宠物状态这种需要精细响应式的场景  |

| \*\*宠物渲染\*\* | Canvas 2D 或 `pet-engine` 组件 | 轻量像素风用 Canvas 足够，有现成 Vue 3 动画引擎可减少造轮子  |

| \*\*后端\*\* | Spring Boot 3 + MyBatis-Plus | Java 生态下最成熟的 CRUD 方案，SQL 可控性强  |

| \*\*实时通信\*\* | Spring WebSocket（STOMP）| 对战场景需要双向实时推送，STOMP 协议上手快  |

| \*\*数据库\*\* | MySQL 8.0 | 状态存 MySQL，对局临时数据可考虑 Redis |



\### Claude Code + DeepSeek 环境配置



根据 DeepSeek 官方文档，需要设置以下环境变量 ：



\*\*Mac / Linux：\*\*

```bash

export ANTHROPIC\_BASE\_URL=https://api.deepseek.com/anthropic

export ANTHROPIC\_AUTH\_TOKEN=<你的 DeepSeek API Key>

export ANTHROPIC\_MODEL=deepseek-flash\[1m]

export ANTHROPIC\_DEFAULT\_OPUS\_MODEL=deepseek-flash\[1m]

export ANTHROPIC\_DEFAULT\_SONNET\_MODEL=deepseek-flash\[1m]

export ANTHROPIC\_DEFAULT\_HAIKU\_MODEL=deepseek-flash

export CLAUDE\_CODE\_SUBAGENT\_MODEL=deepseek-flash

export CLAUDE\_CODE\_EFFORT\_LEVEL=max

export CLAUDE\_CODE\_AUTO\_COMPACT\_WINDOW=786432

```



\*\*Windows（PowerShell）：\*\*

```powershell

$env:ANTHROPIC\_BASE\_URL="https://api.deepseek.com/anthropic"

$env:ANTHROPIC\_AUTH\_TOKEN="<你的 DeepSeek API Key>"

$env:ANTHROPIC\_MODEL="deepseek-flash\[1m]"

$env:ANTHROPIC\_DEFAULT\_OPUS\_MODEL="deepseek-flash\[1m]"

$env:ANTHROPIC\_DEFAULT\_SONNET\_MODEL="deepseek-flash\[1m]"

$env:ANTHROPIC\_DEFAULT\_HAIKU\_MODEL="deepseek-flash"

$env:CLAUDE\_CODE\_SUBAGENT\_MODEL="deepseek-flash"

$env:CLAUDE\_CODE\_EFFORT\_LEVEL="max"

$env:CLAUDE\_CODE\_AUTO\_COMPACT\_WINDOW="786432"

```



设置后执行 `cd /path/to/pet-project \&\& claude` 即可开始 vibe coding 。需要注意 `ANTHROPIC\_MODEL` 的值带 `\[1m]` 后缀，这是 DeepSeek 侧对长上下文窗口的标识。



\### 项目骨架



```

pet-game/

├── pet-web/                        # Vue 3 前端

│   ├── src/

│   │   ├── components/

│   │   │   ├── PetCanvas.vue       # Canvas 宠物渲染

│   │   │   ├── StatusBar.vue       # 饥饿/快乐/健康状态条

│   │   │   └── ActionPanel.vue     # 喂食/玩耍/清洁按钮

│   │   ├── composables/

│   │   │   └── useWebSocket.ts     # WebSocket 连接封装

│   │   ├── stores/

│   │   │   └── petStore.ts         # Pinia 宠物状态

│   │   ├── views/

│   │   │   ├── HomeView.vue        # 主界面

│   │   │   └── BattleView.vue      # 对战界面

│   │   └── router/index.ts

│   └── vite.config.ts

│

├── pet-server/                     # Spring Boot 后端

│   ├── src/main/java/com/yourname/pet/

│   │   ├── controller/

│   │   │   ├── PetController.java  # REST: 喂食/查询状态

│   │   │   └── BattleController.java

│   │   ├── service/

│   │   │   ├── PetService.java     # 状态衰减/进化逻辑

│   │   │   └── BattleService.java  # 对战计算

│   │   ├── config/

│   │   │   └── WebSocketConfig.java

│   │   └── entity/

│   │       ├── Pet.java

│   │       └── BattleRecord.java

│   └── pom.xml

│

└── README.md

```



\### 分阶段实施



\*\*第一阶段：宠物“活起来”（1–2 周）\*\*



目标是让宠物在前端动起来，后端存储状态。前端用 `pet-engine` 或纯 Canvas 实现帧动画（呼吸、眨眼、待机）。后端 `PetService` 提供一个 `GET /api/pet/{userId}` 接口返回宠物当前状态，再用一个 Spring `@Scheduled` 定时任务（比如每 60 秒）按规则衰减饥饿值、快乐度。这个阶段的核心产出是\*\*“你不操作，宠物数值也在变”\*\*的感知。



\*\*第二阶段：养成闭环（1–2 周）\*\*



前端增加喂食、玩耍、清洁按钮，调用后端 `POST /api/pet/feed` 等接口。后端返回更新后的完整状态，前端用 Pinia 更新 UI。进化逻辑建议放在后端：当 `健康 >= 80 \&\& 快乐 >= 80` 时，`evolutionStage` 递增，前端根据这个字段切换 sprite 图集。这个阶段要确保\*\*每次操作都有即时反馈\*\*（按钮动画、数值跳动）。



\*\*第三阶段：对战（2–3 周）\*\*



这是全栈项目里技术含量最高的部分。参考实时多人游戏的成熟架构 ：客户端通过 WebSocket 发送“发起对战/出招”指令，Spring Boot 的 `BattleController` 接收后放入内存队列（或用 Redis），由一个\*\*服务器端游戏循环\*\*（可以用 `@Scheduled(fixedRate)` 或独立的线程）处理回合计算，再通过 WebSocket 推送对战结果给双方。



对战计算逻辑建议简单直接，比如：

```

战斗力 = 健康 \* 0.4 + 快乐 \* 0.3 + 饥饿 \* 0.3

```

再加上宠物品种的克制关系（猫咪克史莱姆、史莱姆克小狗等），让策略性来自“选宠”而非“数值堆砌”。



\### 给 Claude Code 的 Prompt 策略



不要一次性说“给我写完整的前后端”。按照 vibe coding 的节奏分批来：



\*\*第一批（骨架）：\*\*

> “在 `pet-server` 目录下创建一个 Spring Boot 3 项目，包含 `Pet` 实体（id, name, hunger, happiness, health, evolutionStage, species）和对应的 Controller/Service。用 MyBatis-Plus 做 CRUD，数据库用 MySQL。”



\*\*第二批（状态衰减）：\*\*

> “给 `PetService` 添加一个 `@Scheduled` 方法，每 60 秒执行一次：hunger 减 2，happiness 减 3，health 根据前两者计算。hunger 低于 20 时 health 额外扣减。”



\*\*第三批（WebSocket）：\*\*

> “配置 Spring WebSocket，启用 STOMP。创建一个 `/topic/battle/{roomId}` 的广播通道。当客户端发送 `/app/battle/move` 时，服务端处理出招逻辑并广播回合结果。”



每批完成后让 Claude Code 自己跑一遍 `mvn compile` 或 `npm run build`，确保可编译再进入下一批。



\### 一个可以偷懒的加速点



如果你觉得 Canvas 动画从零写太慢，可以先用 `pet-engine` 这个 npm 包把宠物渲染跑通 ，它自带 Vue 3 组件、帧动画、状态机和拖拽交互。你只需要把精力集中在后端状态管理和对战逻辑上，等前端骨架搭好后再替换成自定义 sprite。这样能显著缩短“从零到看到宠物动起来”的时间。

